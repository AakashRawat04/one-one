# Launch Screen Assets

These legacy Flutter launch images are retained for migration history but are no longer referenced by `LaunchScreen.storyboard`. The native launch surface stays blank until the custom Flutter splash renders.
