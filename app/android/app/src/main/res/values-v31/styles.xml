<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Android 12+ always supplies a system launch screen. Make it a blank
         continuation of the custom Flutter splash instead of showing the app icon. -->
    <style name="LaunchTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowSplashScreenBackground">@color/splash_background</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/transparent_splash_icon</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>
</resources>
